[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pixel-brain"
version = "0.1.0"
dependencies = ["numpy", "imageio"]

[project.scripts]
pixel-brain = "pixel_cell_m1:run"
